# patchright is an undetected, drop-in fork of Playwright that avoids the CDP
# Runtime.enable leak anti-bots detect. Pin it to the SAME version as the base
# image (mcr.microsoft.com/playwright/python:v1.48.0-jammy) so its Chromium
# build and the base image's system libraries line up; `patchright install
# chromium` in the Dockerfile then reuses that browser.
aiohttp>=3.9
patchright==1.48.0.post0
