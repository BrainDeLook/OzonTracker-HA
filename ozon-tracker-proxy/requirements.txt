# camoufox is a Firefox-based anti-detect browser: it spoofs the whole
# fingerprint (WebGL, canvas, fonts, navigator, screen) at the browser level,
# which is what defeats challenges that Chromium forks fail under a GPU-less
# virtual display. Its browser binary is fetched in the Dockerfile
# (`python -m camoufox fetch`). It pulls a compatible Playwright itself.
aiohttp>=3.9
camoufox>=0.4.11
